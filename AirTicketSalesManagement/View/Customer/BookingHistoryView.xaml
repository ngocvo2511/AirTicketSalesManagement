<UserControl x:Class="AirTicketSalesManagement.View.Customer.BookingHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:uc ="clr-namespace:AirTicketSalesManagement.View.Customer"
             xmlns:local="clr-namespace:AirTicketSalesManagement.View.Customer"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="980">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0 10 0 20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Lịch sử đặt vé" 
                   FontSize="30" FontWeight="Bold"
                   Margin="30 0 0 0"
                   Foreground="#2C3E50"
                   VerticalAlignment="Center" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <ToggleButton Template="{StaticResource ProfilePopupTemplate}" 
                          Width="60"
                          Height="60"
                          Margin="0 0 40 0"/>
            </StackPanel>
        </Grid>
        <!-- Search Bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" 
            Margin="20 0 20 20" HorizontalAlignment="Left">
            <TextBox x:Name="SearchTextBox" Width="300" Height="35" 
             Margin="0 0 10 0" 
             materialDesign:HintAssist.Hint="Tra cứu lịch sử đặt vé" />
            <Button Width="40" Height="35" Background="#3498DB" 
            Foreground="White" Padding="5">
                <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20"/>
            </Button>
        </StackPanel>

        <!-- Content -->


        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20" Grid.Row="2">
            <StackPanel>
                <TextBlock Text="Không có chuyến bay nào" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center" 
            FontSize="16" 
            FontWeight="Bold"
            Foreground="Gray" 
            Visibility="{Binding IsEmpty, Converter={StaticResource ZeroToVisibleConverter}}"/>
                <ItemsControl ItemsSource="{Binding HistoryBooking}"
                          Visibility="{Binding IsEmpty, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Padding="10" Margin="10,10,10,10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <!-- Nội dung chính -->
                                        <RowDefinition Height="Auto"/>
                                        <!-- Ngày đặt -->
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="3*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Cột 1: Điểm đi -> Điểm đến -->
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DiemDi}" FontSize="22" FontWeight="Bold"/>
                                            <materialDesign:PackIcon Kind="ArrowRight" Margin="10,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding DiemDen}" FontSize="22" FontWeight="Bold"/>
                                        </StackPanel>

                                        <!-- Cột 2: Hãng, thời gian, máy bay -->
                                        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="20,0,0,0">
                                            <TextBlock Text="{Binding HangHangKhong}"
                                                FontSize="18"
                                                FontWeight="SemiBold"
                                                Foreground="#007ACC"
                                                Margin="0,5"/>
                                            <TextBlock Text="{Binding GioDi, StringFormat='🕒 Giờ đi: {0:HH:mm dd/MM/yyyy}'}"
                                                FontSize="16"
                                                Margin="0,5"
                                                />
                                            <TextBlock Text="{Binding GioDen, StringFormat='🕒 Giờ đến: {0:HH:mm dd/MM/yyyy}'}"
                                                FontSize="16"
                                                Margin="0,5"/>
                                            <TextBlock FontSize="16" Margin="0,5">
                                            <Run Text="🛩 Loại máy bay: "/>
                                            <Run Text="{Binding LoaiMayBay}" />
                                            </TextBlock>
                                        </StackPanel>

                                        <!-- Cột 3: Số lượng khách -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="15,0,0,0">
                                            <TextBlock>
                                              <Run Text="👥 Số lượng khách: " FontSize="16"/>
                                              <Run Text="{Binding SoLuongKhach}" FontSize="16"/>
                                            </TextBlock>
                                           
                                        </StackPanel>

                                        <!-- Cột 4: Nút Chi tiết -->
                                        <Button Grid.Column="3"
                    Content="Chi tiết"
                    Padding="10,4"
                    Margin="15,0,0,0"
                    Command="{Binding DataContext.ShowDetailHistoryCommand}"
                    CommandParameter="{Binding}"
                    Background="#007ACC"
                    Foreground="White"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    VerticalAlignment="Center"/>
                                    </Grid>

                                    <!-- Ngày đặt - góc dưới trái -->
                                    <TextBlock Grid.Row="1"
                   Text="{Binding NgayDat, StringFormat='Ngày đặt: {0:dd/MM/yyyy}'}"
                   FontStyle="Italic"
                   FontSize="12"
                   Foreground="Gray"
                   Margin="0,5,0,0"
                   HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>

                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>
