<UserControl x:Class="AirTicketSalesManagement.View.Notification.NotificationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400">
    <UserControl.Resources>
        <Style x:Key="NotificationBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding NotificationType}" Value="Information">
                    <Setter Property="BorderBrush" Value="Blue"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding NotificationType}" Value="Warning">
                    <Setter Property="BorderBrush" Value="Orange"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding NotificationType}" Value="Error">
                    <Setter Property="BorderBrush" Value="Red"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding NotificationType}" Value="Confirmation">
                    <Setter Property="BorderBrush" Value="Green"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border Background="#80000000" CornerRadius="5" Padding="10">
        <Border Style="{StaticResource NotificationBorderStyle}" CornerRadius="5" BorderThickness="1">
            <StackPanel Margin="10">
                <TextBlock Text="{Binding Message}" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="OK" Width="80" Margin="0,0,10,0" Command="{Binding OkCommand}"/>
                    <Button Content="Hủy" Width="80" Command="{Binding CancelCommand}" Visibility="{Binding ShowCancelButton, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Border>
</UserControl>
